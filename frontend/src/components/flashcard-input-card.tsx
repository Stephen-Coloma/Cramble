import { Card, CardContent } from "./ui/card";

export function FlashcardInputCard(){
    return(
        <Card className="w-full h-[300px] bg-muted">
            <CardContent>
                
            </CardContent>
        </Card>
    )
}